[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "snake-terminal"
version = "0.3.0"
description = "Jeu Snake jouable hors-ligne dans le terminal."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
